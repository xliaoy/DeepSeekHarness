# 清理无用文件记录（2026-09-15）

共删除 **24 个文件**（13 drawable + 1 layout + 10 assets 顶层），删除前全部备份到 `/root/deleted-backup-09151919`，编译验证通过。

## 一、删除清单

| 文件 | 类型 | 说明 |
|---|---|---|
| bg_selection.xml | drawable | 终端标签选中底，已由 bg_chip_selected 取代（TabBar 改主色填充） |
| bg_tab.xml / bg_tab_on.xml | drawable | 插件页旧 tab 切换背景，布局重构为搜索+筛选卡后废弃 |
| checkbox_custom.xml | drawable | 自绘 checkbox 方案（改用 Material3 标准对勾后废弃） |
| ic_btn_play/restart/stop.xml、ic_ply/rst/stp.xml、ic_play_big.xml、ic_restart_new/stop_new.xml | drawable | 启动页手势按钮旧图标（SlidePanel 改用 ic_rst2/ic_ply2/ic_stp2） |
| dialog_remind_backup.xml | layout | 备份提醒弹窗旧布局（改由 AppDialogs 通用弹窗） |
| dsh-deps-heal.sh 等 10 个 | assets | 历史遗留脚本/清单（heal/migrate/pnpm-env/webui-patch 等），运行时无引用 |

## 二、各删除文件完整内容

> 完整内容已备份在 `/root/deleted-backup-09151919`，此处附关键文件内容；全部内容可随时取回。


### `bg_selection.xml`
```xml
<?xml version="1.0" encoding="utf-8"?>
<!-- 终端标签选中态：iOS 分组灰底（raised）+ 控制圆角 -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/raised" />
    <corners android:radius="@dimen/radius_control" />
</shape>
```

### `checkbox_custom.xml`
```xml
<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="true" android:drawable="@drawable/ic_checkbox_checked" />
    <item android:drawable="@drawable/ic_checkbox_unchecked" />
</selector>
```

### `dialog_remind_backup.xml`
```xml
<?xml version="1.0" encoding="utf-8"?>
<!-- 备份提醒弹窗：iOS 拟态卡片分组（白色大圆角卡片承载可点条目） -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/surface"
    android:paddingLeft="@dimen/page_pad"
    android:paddingRight="@dimen/page_pad"
    android:paddingTop="@dimen/gap_tight"
    android:paddingBottom="@dimen/gap_tight">

    <LinearLayout
        style="@style/DeepSeekHarnessCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <CheckBox
            android:id="@+id/remind_less"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:minHeight="@dimen/touch_min"
            android:gravity="center_vertical"
            android:paddingLeft="@dimen/gap_hair"
            android:paddingRight="@dimen/gap_hair"
            android:text="@string/dialog_remind_less"
            android:textColor="@color/text"
            android:textSize="@dimen/text_body" />
    </LinearLayout>
</LinearLayout>
```

## 三、相关代码（删除原因对照）

```java
// TerminalTabBar 选中已改用 bg_chip_selected + primary 填充（原 bg_selection 灰底废弃）
chip.setBackgroundResource(selected ? R.drawable.bg_chip_selected : R.drawable.bg_chip);

// 插件页布局重构：删除 tab 切换（btnMarket/btnInstalled）→ 搜索+筛选卡
// 旧引用 R.id.pluginMarketCard / pluginWebsiteSection / pluginLinkSection / marketHelp / installedControls 已随布局移除
```
