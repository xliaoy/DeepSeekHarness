# dsh-status-overlay bundle patch: one host-side plugin row.
# Pure event listener — no module-level `inject`, so it can never leave the entry
# pending and take the whole plugin tree down with it.
- insert:
    - id: dsha-status-overlay
      name: 'dsh-status-overlay'
