/**
 * dsh-balance-panel — cordis host entry placeholder.
 * Actual host body lives in lib/host.js (webServer route), resolved via the
 * package.json `main` / `exports["."]`; this file only satisfies the built-in
 * deployment manifest that copies lib/index.js for every builtin plugin.
 */
function apply() {}
export { apply };
