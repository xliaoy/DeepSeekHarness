<manifest xmlns:android="http://schemas.android.com/apk/res/android" xmlns:tools="http://schemas.android.com/tools">
    <!-- terminal-view/emulator 0.118 的 Java API 已核查：O 的输入法回调仅由新系统调用；JNI 用 API 23 重编。 -->
    <uses-sdk tools:overrideLibrary="com.termux.view,com.termux.terminal" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <application android:requestLegacyExternalStorage="true">
        <activity android:name=".ui.GeckoPreviewActivity" android:exported="false"
            android:windowSoftInputMode="adjustResize" android:theme="@style/Theme.DeepseekHarness" />
    </application>
</manifest>
